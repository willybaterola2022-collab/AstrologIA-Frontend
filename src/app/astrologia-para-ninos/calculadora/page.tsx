import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Calculadora Carta Natal Niños — AstroKids",
    description: "Calcula la carta natal de tu hijo gratis. Descubre su signo solar, lunar y ascendente. Interpretación infantil personalizada.",
};

// Stub — Lovable component will be imported here after migration
export default function CalculadoraPage() {
    return (
        <div style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", background: "#0a0a0f", color: "white" }}>
            <p>Calculadora — Migración Lovable pendiente</p>
        </div>
    );
}
